# REPRODUCIBILITY

이 저장소는 두 가지를 목표로 정리했다.

- `public/data`를 이용해 대시보드를 바로 실행할 수 있어야 한다.
- 원천 데이터가 있을 경우 `scripts/`와 `derived_data/`를 따라 처리 흐름을 이해할 수 있어야 한다.

## 1. 실행 방법

```bash
npm install
npm run dev
```

브라우저에서 표시되는 대시보드는 `public/data/`의 최종 산출물을 사용한다. 원천 데이터가 없어도 실행은 가능하다.

## 2. 빌드 방법

```bash
npm run build
```

## 3. 데이터 재생성 원칙

- GitHub에는 대용량 원천 데이터가 포함되지 않는다.
- `public/data/`는 이미 생성된 최종 대시보드 데이터이므로, 원천 데이터가 없어도 대시보드 실행은 가능하다.
- 전체 재현은 원천 데이터를 각 출처에서 다시 내려받은 뒤, `scripts/README.md`에 적힌 순서대로 스크립트를 실행하는 방식이다.
- 이번 정리 작업에서는 `public/data`를 재생성하지 않았고, 기존 최종 값도 임의 변경하지 않았다.

## 4. 원천 데이터 필요 목록

| 데이터 | 출처 | 기준시점 | 저장 위치(로컬) | 비고 |
| -- | -- | -- | -- | -- |
| 용도지역2 | 토지이음 | 2024년 1월 갱신본 | `용도지역2/` | 원천 SHP, GitHub 제외 |
| 용도지역3 | 토지이음 | 2024년 2월 갱신본 | `용도지역3/` | 원천 SHP, GitHub 제외 |
| 용도지역 전국 ZIP | 토지이음 | 2024년 1월 | `용도지역/` | 전국 ZIP/CSV, GitHub 제외 |
| SGIS 인구·가구 | SGIS | 2023년 | `인구가구/` | 집계구 통계 |
| SGIS 사업체·종사자 | SGIS | 2023년 | `인구가구/` | 산업분류별 CSV |
| SGIS 집계구 경계 | SGIS | 2025년 2분기 | `집계구경계/` | SHP, 공간 배분용 |
| 연속지적도 | 공공데이터 기반 | 2026년 5~6월 확보본 | `연속지적도/` | 필지 분석용 SHP |
| 건축물대장 | 공공데이터 기반 | 2026년 6월 확보본 | `건축물/` | 2023년 이전 승인분 필터링 |
| 가구 및 획지 | 프로젝트 확보 자료 | 파일 기준 | `가구및획지/` | 경계 추출 원천 |
| OSM 도로망 | OpenStreetMap 기반 | 파일 기준 | `derived_data/04_accessibility/osm_road_network/` 또는 별도 원천 | 최종 가공본은 저장소 포함 |
| 대중교통 네트워크 | 프로젝트 파일 기준 | 파일 기준 | `network/`, `subway/`, `edit/` | 접근성 분석용 |
| 판교 계획도면 | 프로젝트 확보 자료 | 파일 기준 | `판교/` | DWG 원천, GitHub 제외 |
| 위례 계획자료 | 프로젝트 확보 자료 | 파일 기준 | `위례/` | 보조 원천, GitHub 제외 권장 |

## 5. 재현 절차 요약

1. 위 표의 원천 데이터를 각 로컬 폴더에 준비한다.
2. Python GIS 실행 환경을 갖춘다.
3. `scripts/README.md`의 순서를 따라 경계, 토지이용, 개발실현도, SGIS, 접근성 스크립트를 실행한다.
4. 결과는 `derived_data/`에 생성되고, 최종 대시보드용 파일은 `public/data/`에 반영된다.

## 6. 재현 시 유의사항

- 스크립트는 현재 저장소 루트 기준 상대경로를 사용한다.
- 원천 데이터 폴더명은 현재 프로젝트의 기존 이름을 기준으로 작성되어 있다.
- 일부 검증용 스크립트는 최종 산출물 재생성보다 품질 확인 목적이다.
- `analysis_boundaries/`의 GeoJSON은 경계 편집/검토용 보조 자료다.
- `backup_before_pangyo_boundary_update/`, `dashboard/`, `archive/`, `public/data/archive/`는 제출 대상이 아니며 GitHub에서 제외된다.

